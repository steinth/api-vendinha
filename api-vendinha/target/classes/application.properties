# Define o nome da aplica��o, que pode ser usado para identific�-la em logs ou quando v�rias aplica��es est�o rodando.
spring.application.name=api

# Define a porta na qual o servidor embutido (por exemplo, Tomcat) vai escutar as requisi��es HTTP.
server.port=8081

# Configura��o da URL do banco de dados. Neste caso, � um banco de dados H2 em mem�ria com o nome "vendinha".
spring.datasource.url=jdbc:h2:mem:vendinha

# Define a classe do driver JDBC para o banco de dados H2.
spring.datasource.driverClassName=org.h2.Driver

# Configura o nome de usu�rio para se conectar ao banco de dados H2.
spring.datasource.username=sa

# Configura a senha para se conectar ao banco de dados H2.
spring.datasource.password=password

# Define o dialeto do Hibernate para o banco de dados H2. Isso permite que o Hibernate gerencie a cria��o e manipula��o do banco de dados de acordo com as caracter�sticas do H2.
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# Habilita o console web do H2, que permite acessar e gerenciar o banco de dados H2 atrav�s de uma interface gr�fica.
spring.h2.console.enabled=true

# Define o caminho para acessar o console do H2 na web. Com essa configura��o, o console estar� dispon�vel em http://localhost:8081/h2-console.
spring.h2.console.path=/h2-console
